{
  "name": "lodash-to-method-module",
  "version": "0.1.0",
  "description": "Convert .ts lodash imports to module level",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && jasmine src/**/*_spec.js"
  },
  "keywords": [
    "schematics"
  ],
  "author": "",
  "license": "MIT",
  "schematics": "./src/collection.json",
  "dependencies": {
    "tsmisc": "^1.7.1"
  },
  "devDependencies": {
    "@types/node": "^10.12.3",
    "typescript": ">= 3.0.0"
  },
  "peerDependencies": {
    "typescript": ">= 2.5.0"
  }
}
